import styles from './Ecology.module.scss';
import EcologySlider from '.././../ui/Slider/EcologySlider';

const Ecology = () => {
	return (
		<div className={styles.ecology}>
			<EcologySlider />
		</div>
	);
};

export default Ecology;
